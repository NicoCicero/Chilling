import "./globals.css";

export const metadata = {
  title: "CHILLING.OG — Leaderboard",
  description: "Leaderboard semanal 4:20"
};

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  );
}
